# PLAN

## AI 자동화 섹션

### 목표
- `AIAutomationSection.tsx`의 결과 출력 예시에서 필터 상태가 실제 컬럼 출력과 일치하도록 개선한다.
- `웨폰 타입` 열 너비 조절 시 인접 `쇼` 컬럼 레이아웃이 깨지지 않도록 안정화한다.

### 작업 체크리스트
- [x] 현재 결과 출력 예시 렌더링 구조 파악 (`무기 타입`, `열 표시 필터`, 체크박스 기반 표시 로직 확인)
- [x] `무기 타입`과 `열 표시 필터`의 체크박스 항목을 결과 영역에서 드롭다운 항목 형태로 매핑하는 데이터 구조 설계
- [x] 결과 출력 예시 UI를 체크박스 나열 방식에서 드롭다운 항목 출력 방식으로 변경
- [x] 드롭다운 항목 변경 시 기존 필터 상태(선택/해제)가 결과 출력에 동일하게 반영되는지 검증
- [x] `웨폰 타입` 열의 최소/최대 너비 및 overflow 처리 정책 정의
- [x] `웨폰 타입` 셀 내용에 `text-overflow`, `white-space`, `overflow` 및 필요 시 `word-break` 적용
- [x] 테이블/그리드 컬럼 폭 계산 로직 조정으로 `쇼` 컬럼 침범 방지
- [x] 열 너비 수동 조절(축소/확대) 시 `쇼` 컬럼 고정/가시성 유지 동작 점검
- [x] 반응형 구간(모바일/태블릿/데스크톱)에서 컬럼 겹침 여부 최종 확인

### 완료 조건
- 결과 출력 예시에서 `무기 타입`과 `열 표시 필터` 체크박스 항목이 모두 드롭다운 항목 형태로 표시된다.
- `웨폰 타입` 열 너비를 변경해도 데이터가 `쇼` 컬럼 영역을 침범하지 않는다.

## 자잘한 버그 수정

### 목표
- `무기별로 보기` 필터의 기본 표시값을 `전체`로 통일하고, 드롭다운 내부에서 개별 무기 체크박스 선택이 가능하도록 개선한다.
- `열 표시` 필터도 동일하게 기본 표시값을 `전체`로 통일하고, 항목별 체크박스 및 선택 항목 라벨 노출을 일관되게 맞춘다.

### 작업 체크리스트
- [x] `무기별로 보기` 필터 라벨 계산 로직 정리 (모든 무기 선택 시 `전체` 표시)
- [x] `무기별로 보기` 드롭다운 항목 앞 체크박스 UI 추가 (사슬검, 쌍도끼, 전투도끼)
- [x] 무기 체크박스 true/false 변경 시 결과 테이블 필터 반영 검증
- [x] 일부 무기만 선택된 경우 필터 버튼에 선택된 무기명(요약 텍스트) 출력
- [x] `열 표시` 필터 라벨 계산 로직 정리 (모든 열 선택 시 `전체` 표시)
- [x] `열 표시` 드롭다운 항목 앞 체크박스 UI 추가
- [x] 열 체크박스 변경 시 컬럼 표시/숨김 동작 및 필터 버튼 선택 항목 텍스트 반영 검증
- [x] 무기/열 필터 모두 미선택 또는 부분선택 상태에서 예외 표시 문구/동작 최종 점검

### 완료 조건
- `무기별로 보기` 기본값이 `전체`로 표시되고, 드롭다운 내부에서 항목별 체크박스 선택이 가능하다.
- `무기별로 보기`에서 선택된 무기 상태가 결과 필터와 버튼 라벨에 정확히 반영된다.
- `열 표시` 기본값이 `전체`로 표시되고, 드롭다운 내부에서 항목별 체크박스 선택이 가능하다.
- `열 표시`에서 선택된 열 상태가 컬럼 가시성과 버튼 라벨에 정확히 반영된다.
